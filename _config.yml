title: Traffic Torch
description: Instant 360° SEO and UX health score Analysis, report and education website.

url: "https://traffictorch.github.io"  # Replace with custom domain if you switch later
baseurl: ""                           # Leave empty for root domain or project site

plugins:
  - jekyll-sitemap
  - jekyll-seo-tag

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post