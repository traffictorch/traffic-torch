export function analyzeContentExtractability(doc, html, text) {
  // Stub: return a score and metrics
  const score = 70;
  const metrics = { semanticHtml: true, headingHierarchy: true, divSoupDensity: 0.3 };
  const failed = [];
  return { score, metrics, failed };
}
