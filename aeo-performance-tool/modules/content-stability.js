export function analyzeContentStability() {
  // Stub
  const score = 70;
  const metrics = { lateInjection: false, hydrationFlicker: false };
  const failed = [];
  return { score, metrics, failed };
}
