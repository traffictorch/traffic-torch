export function analyzeRenderFidelity(renderedHtml, rawHtml) {
  // Stub
  const score = 70;
  const metrics = { renderMatch: true, hydrationMismatches: 0, jsErrors: 0 };
  const failed = [];
  return { score, metrics, failed };
}
