export function analyzeCrawlerAccessibility(html, doc, url) {
  // Stub
  const score = 75;
  const metrics = { robotsFriendly: true, jsGated: false, infiniteScroll: false };
  const failed = [];
  return { score, metrics, failed };
}
