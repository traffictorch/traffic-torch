export function analyzeSchemaParse(html, doc) {
  // Stub
  const score = 65;
  const metrics = { parseSuccess: true, errors: 0, warnings: 0 };
  const failed = [];
  return { score, metrics, failed };
}
